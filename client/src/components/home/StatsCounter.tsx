import { useTranslation } from '../../hooks/useTranslation';
import { useEffect, useState, useRef } from 'react';

interface StatItemProps {
  end: number;
  suffix: string;
  label: string;
  icon: React.ReactNode;
}

function StatItem({ end, suffix, label, icon }: StatItemProps) {
  const [count, setCount] = useState(0);
  const ref = useRef<HTMLDivElement>(null);
  const counted = useRef(false);

  useEffect(() => {
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting && !counted.current) {
          counted.current = true;
          const duration = 2000;
          const steps = 60;
          const increment = end / steps;
          let current = 0;
          const timer = setInterval(() => {
            current += increment;
            if (current >= end) {
              setCount(end);
              clearInterval(timer);
            } else {
              setCount(Math.floor(current));
            }
          }, duration / steps);
        }
      },
      { threshold: 0.3 }
    );

    if (ref.current) observer.observe(ref.current);
    return () => observer.disconnect();
  }, [end]);

  return (
    <div ref={ref} className="text-center">
      <div className="mx-auto mb-4 flex h-16 w-16 items-center justify-center rounded-2xl bg-white/10">
        {icon}
      </div>
      <div className="text-4xl font-bold text-white">
        {count}
        <span className="text-bafco-gold">{suffix}</span>
      </div>
      <p className="mt-2 text-sm text-white/60">{label}</p>
    </div>
  );
}

export default function StatsCounter() {
  const { t } = useTranslation();

  const stats = [
    { end: 5, suffix: '+', label: t('home.stats.factories'), icon: <svg className="h-8 w-8 text-bafco-gold" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={1.5} d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" /></svg> },
    { end: 150, suffix: '+', label: t('home.stats.products'), icon: <svg className="h-8 w-8 text-bafco-gold" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={1.5} d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4" /></svg> },
    { end: 3000, suffix: '+', label: t('home.stats.employees'), icon: <svg className="h-8 w-8 text-bafco-gold" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={1.5} d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" /></svg> },
    { end: 45, suffix: '+', label: t('home.stats.depots'), icon: <svg className="h-8 w-8 text-bafco-gold" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={1.5} d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" /></svg> },
  ];

  return (
    <section className="relative bg-gradient-to-r from-bafco-navy via-bafco-red to-bafco-navy py-20">
      <div className="absolute inset-0 bg-[url('data:image/svg+xml;base64,')] opacity-50" />
      <div className="container-bafco relative">
        <h2 className="text-center text-3xl font-bold text-white mb-12">{t('home.stats.title')}</h2>
        <div className="grid grid-cols-2 gap-8 md:grid-cols-4">
          {stats.map((stat) => (
            <StatItem key={stat.label} {...stat} />
          ))}
        </div>
      </div>
    </section>
  );
}
